@using Euclid.Composites.Mvc.Models
@model IEnumerable<CommandMetadataModel>

<div class="model-header">@ViewBag.Title</div>

<ul>
    <li>
        @Model.ElementAt(0).AgentInfo.CommandNamespace
        <ul>
        @foreach (var m in Model)
        {
            var agentMetadata = m.AgentInfo;
            <li>
                <a href="@Url.Action("Inspect", "Command", new { systemName = agentMetadata.SystemName, command = m.CommandMetadata.Name })">
                   @m.CommandMetadata.Name
                </a>
            </li>
        }
        </ul>
    </li>
</ul>
